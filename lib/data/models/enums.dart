enum AppLanguage { en, hi }

enum ProfileRelation { self, mother, father, child, spouse, other }

enum MealTiming { beforeMeal, afterMeal, anyTime }

enum DoseStatus { pending, taken, skipped }

enum VitalsType { bloodPressure, bloodSugar }

enum MoodLevel { veryLow, low, neutral, good, great }
